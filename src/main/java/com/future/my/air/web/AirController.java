package com.future.my.air.web;

import com.future.my.air.service.AirService;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.*;

import java.util.List;
import java.util.Map;

//@Controller
//@RequestMapping("/air")
public class AirController {
/*
    @Autowired
    private AirService airService;

    // JSP 화면 반환
    @GetMapping
    public String airView() {
        return "air/airView"; // /WEB-INF/views/air/airView.jsp
    }

    // JSON API: 시/도별 측정소 + 미세먼지 데이터
    @GetMapping("/{sido}")
    @ResponseBody
    public List<Map<String, Object>> getAirInfo(@PathVariable String sido) {
        // 그냥 API에서 가져온 측정소 리스트 반환
        return airService.getSidoAirQualityList(sido);
    }
*/
}

